<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="SubExplore.Views.Admin.SpotValidationPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SubExplore.ViewModels.Admin"
             xmlns:models="clr-namespace:SubExplore.Models.Domain"
             x:DataType="vm:SpotValidationViewModel"
             Title="{Binding Title}"
             Shell.BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}">

    <Shell.TitleView>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <!-- Back Button -->
            <Button Grid.Column="0"
                    Text="◀"
                    FontSize="20"
                    TextColor="White"
                    BackgroundColor="Transparent"
                    BorderColor="Transparent"
                    WidthRequest="40"
                    HeightRequest="40"
                    Margin="5,0"
                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SpotValidationViewModel}}, Path=GoBackCommand}" />
            
            <!-- Title -->
            <Label Grid.Column="1"
                   Text="Validation des Spots" 
                   FontSize="18" 
                   FontAttributes="Bold" 
                   TextColor="White" 
                   VerticalOptions="Center"
                   HorizontalOptions="Center" />
            
            <!-- Home Button -->
            <Button Grid.Column="2"
                    Text="🏠"
                    FontSize="18"
                    TextColor="White"
                    BackgroundColor="Transparent"
                    BorderColor="Transparent"
                    WidthRequest="40"
                    HeightRequest="40"
                    Margin="5,0"
                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SpotValidationViewModel}}, Path=GoToHomeCommand}" />
        </Grid>
    </Shell.TitleView>

    <ScrollView>
        <StackLayout Spacing="0">
            <!-- Stats Header -->
            <Frame BackgroundColor="{StaticResource Primary}" 
                   Padding="20,15"
                   CornerRadius="0"
                   IsVisible="{Binding ValidationStats, Converter={StaticResource IsNotNullConverter}}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackLayout Grid.Column="0" HorizontalOptions="Center">
                        <Label Text="{Binding ValidationStats.PendingCount}" 
                               FontSize="28" 
                               FontAttributes="Bold" 
                               TextColor="White" 
                               HorizontalOptions="Center" />
                        <Label Text="En attente" 
                               FontSize="13" 
                               TextColor="White" 
                               HorizontalOptions="Center" />
                    </StackLayout>

                    <StackLayout Grid.Column="1" HorizontalOptions="Center">
                        <Label Text="{Binding ValidationStats.UnderReviewCount}" 
                               FontSize="28" 
                               FontAttributes="Bold" 
                               TextColor="White" 
                               HorizontalOptions="Center" />
                        <Label Text="En révision" 
                               FontSize="13" 
                               TextColor="White" 
                               HorizontalOptions="Center" />
                    </StackLayout>

                    <StackLayout Grid.Column="2" HorizontalOptions="Center">
                        <Label Text="{Binding ValidationStats.SafetyFlaggedCount}" 
                               FontSize="28" 
                               FontAttributes="Bold" 
                               TextColor="White" 
                               HorizontalOptions="Center" />
                        <Label Text="Sécurité" 
                               FontSize="13" 
                               TextColor="White" 
                               HorizontalOptions="Center" />
                    </StackLayout>
                </Grid>
            </Frame>

            <!-- Section: Spots en attente de validation -->
            <StackLayout>
                <Frame BackgroundColor="White" 
                       Padding="0"
                       CornerRadius="15"
                       Margin="15,10">
                    <StackLayout>
                        <!-- Header -->
                        <Grid BackgroundColor="{StaticResource Primary}" 
                              Padding="20,15">
                            <Label Text="Spots en attente de validation" 
                                   FontSize="18" 
                                   FontAttributes="Bold" 
                                   TextColor="White" />
                        </Grid>

                        <!-- Pending Spots List -->
                        <StackLayout>
                            <!-- Empty State -->
                            <StackLayout IsVisible="{Binding PendingSpots.Count, Converter={StaticResource IntToBoolConverter}, ConverterParameter=0}"
                                         Padding="40,60">
                                <Label Text="📋" 
                                       FontSize="48" 
                                       HorizontalOptions="Center" 
                                       TextColor="{StaticResource Gray400}" />
                                <Label Text="Aucun spot en attente de validation" 
                                       FontSize="16" 
                                       HorizontalOptions="Center" 
                                       TextColor="{StaticResource Gray600}" 
                                       Margin="0,10,0,0" />
                                <Label Text="Tous les spots ont été traités" 
                                       FontSize="14" 
                                       HorizontalOptions="Center" 
                                       TextColor="{StaticResource Gray500}" />
                            </StackLayout>

                            <!-- Spots List -->
                            <CollectionView ItemsSource="{Binding PendingSpots}"
                                            IsVisible="{Binding PendingSpots.Count, Converter={StaticResource IntToBoolConverter}, ConverterParameter=1}"
                                            SelectionMode="None">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate x:DataType="models:Spot">
                                        <Frame BackgroundColor="White" 
                                               Padding="0" 
                                               CornerRadius="12" 
                                               Margin="15,8" 
                                               HasShadow="True">
                                            <StackLayout>
                                                <Grid Padding="20,15">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto" />
                                                        <RowDefinition Height="Auto" />
                                                        <RowDefinition Height="Auto" />
                                                    </Grid.RowDefinitions>

                                                    <Label Grid.Row="0" Grid.Column="0"
                                                           Text="{Binding Name}"
                                                           FontSize="18"
                                                           FontAttributes="Bold" 
                                                           TextColor="{StaticResource Primary}" />

                                                    <Label Grid.Row="1" Grid.Column="0"
                                                           Text="{Binding Description}"
                                                           FontSize="14"
                                                           TextColor="{StaticResource Gray600}"
                                                           MaxLines="3" 
                                                           Margin="0,5,0,0" />

                                                    <Label Grid.Row="2" Grid.Column="0"
                                                           Text="{Binding CreatedAt, StringFormat='Soumis le {0:dd/MM/yyyy à {0:HH:mm}'}"
                                                           FontSize="12"
                                                           TextColor="{StaticResource Gray500}" 
                                                           Margin="0,8,0,0" />

                                                    <Frame Grid.Row="0" Grid.Column="1" Grid.RowSpan="3"
                                                           BackgroundColor="Orange"
                                                           Padding="12,6"
                                                           CornerRadius="15"
                                                           VerticalOptions="Center">
                                                        <Label Text="⏳ Nouveau"
                                                               TextColor="White"
                                                               FontSize="13"
                                                               FontAttributes="Bold" />
                                                    </Frame>
                                                </Grid>

                                                <!-- Action Buttons -->
                                                <BoxView BackgroundColor="{StaticResource Gray200}" HeightRequest="1" />
                                                <Grid Padding="15,10">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>

                                                    <Button Grid.Column="0"
                                                            Text="✅ Approuver"
                                                            BackgroundColor="Green"
                                                            TextColor="White"
                                                            FontSize="13"
                                                            Padding="8,10"
                                                            CornerRadius="8"
                                                            Margin="0,0,5,0"
                                                            Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SpotValidationViewModel}}, Path=ApproveSpotCommand}"
                                                            CommandParameter="{Binding .}" />

                                                    <Button Grid.Column="1"
                                                            Text="📋 Réviser"
                                                            BackgroundColor="{StaticResource Primary}"
                                                            TextColor="White"
                                                            FontSize="13"
                                                            Padding="8,10"
                                                            CornerRadius="8"
                                                            Margin="2.5,0"
                                                            Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SpotValidationViewModel}}, Path=AssignForReviewCommand}"
                                                            CommandParameter="{Binding .}" />

                                                    <Button Grid.Column="2"
                                                            Text="❌ Rejeter"
                                                            BackgroundColor="Red"
                                                            TextColor="White"
                                                            FontSize="13"
                                                            Padding="8,10"
                                                            CornerRadius="8"
                                                            Margin="5,0,0,0"
                                                            Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SpotValidationViewModel}}, Path=RejectSpotCommand}"
                                                            CommandParameter="{Binding .}" />
                                                </Grid>
                                            </StackLayout>
                                        </Frame>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </StackLayout>
                    </StackLayout>
                </Frame>
            </StackLayout>

            <!-- Section: Spots en cours de révision -->
            <StackLayout IsVisible="{Binding SpotsUnderReview.Count, Converter={StaticResource IntToBoolConverter}, ConverterParameter=1}">
                <Frame BackgroundColor="White" 
                       Padding="0"
                       CornerRadius="15"
                       Margin="15,10">
                    <StackLayout>
                        <Grid BackgroundColor="Blue" 
                              Padding="20,15">
                            <Label Text="Spots en cours de révision" 
                                   FontSize="18" 
                                   FontAttributes="Bold" 
                                   TextColor="White" />
                        </Grid>

                        <CollectionView ItemsSource="{Binding SpotsUnderReview}"
                                        SelectionMode="None">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="models:Spot">
                                    <Frame BackgroundColor="White" 
                                           Padding="0" 
                                           CornerRadius="12" 
                                           Margin="15,8" 
                                           HasShadow="True">
                                        <StackLayout>
                                            <Grid Padding="20,15">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>

                                                <Label Grid.Row="0" Grid.Column="0"
                                                       Text="{Binding Name}"
                                                       FontSize="18"
                                                       FontAttributes="Bold" 
                                                       TextColor="{StaticResource Primary}" />

                                                <Label Grid.Row="1" Grid.Column="0"
                                                       Text="{Binding Description}"
                                                       FontSize="14"
                                                       TextColor="{StaticResource Gray600}"
                                                       MaxLines="3" 
                                                       Margin="0,5,0,0" />

                                                <Label Grid.Row="2" Grid.Column="0"
                                                       Text="{Binding CreatedAt, StringFormat='En révision depuis le {0:dd/MM/yyyy}'}"
                                                       FontSize="12"
                                                       TextColor="{StaticResource Gray500}" 
                                                       Margin="0,8,0,0" />

                                                <Frame Grid.Row="0" Grid.Column="1" Grid.RowSpan="3"
                                                       BackgroundColor="Blue"
                                                       Padding="12,6"
                                                       CornerRadius="15"
                                                       VerticalOptions="Center">
                                                    <Label Text="🔍 En révision"
                                                           TextColor="White"
                                                           FontSize="13"
                                                           FontAttributes="Bold" />
                                                </Frame>
                                            </Grid>

                                            <BoxView BackgroundColor="{StaticResource Gray200}" HeightRequest="1" />
                                            <Grid Padding="15,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>

                                                <Button Grid.Column="0"
                                                        Text="✅ Approuver"
                                                        BackgroundColor="Green"
                                                        TextColor="White"
                                                        FontSize="13"
                                                        Padding="8,10"
                                                        CornerRadius="8"
                                                        Margin="0,0,5,0"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SpotValidationViewModel}}, Path=ApproveSpotCommand}"
                                                        CommandParameter="{Binding .}" />

                                                <Button Grid.Column="1"
                                                        Text="❌ Rejeter"
                                                        BackgroundColor="Red"
                                                        TextColor="White"
                                                        FontSize="13"
                                                        Padding="8,10"
                                                        CornerRadius="8"
                                                        Margin="5,0,0,0"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SpotValidationViewModel}}, Path=RejectSpotCommand}"
                                                        CommandParameter="{Binding .}" />
                                            </Grid>
                                        </StackLayout>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </StackLayout>
                </Frame>
            </StackLayout>

            <!-- Section: Spots nécessitant une révision de sécurité -->
            <StackLayout IsVisible="{Binding SafetyFlaggedSpots.Count, Converter={StaticResource IntToBoolConverter}, ConverterParameter=1}">
                <Frame BackgroundColor="White" 
                       Padding="0"
                       CornerRadius="15"
                       Margin="15,10">
                    <StackLayout>
                        <Grid BackgroundColor="Purple" 
                              Padding="20,15">
                            <Label Text="Spots nécessitant une révision de sécurité" 
                                   FontSize="18" 
                                   FontAttributes="Bold" 
                                   TextColor="White" />
                        </Grid>

                        <CollectionView ItemsSource="{Binding SafetyFlaggedSpots}"
                                        SelectionMode="None">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="models:Spot">
                                    <Frame BackgroundColor="White" 
                                           Padding="0" 
                                           CornerRadius="12" 
                                           Margin="15,8" 
                                           HasShadow="True">
                                        <StackLayout>
                                            <Grid Padding="20,15">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>

                                                <Label Grid.Row="0" Grid.Column="0"
                                                       Text="{Binding Name}"
                                                       FontSize="18"
                                                       FontAttributes="Bold" 
                                                       TextColor="{StaticResource Primary}" />

                                                <Label Grid.Row="1" Grid.Column="0"
                                                       Text="{Binding Description}"
                                                       FontSize="14"
                                                       TextColor="{StaticResource Gray600}"
                                                       MaxLines="3" 
                                                       Margin="0,5,0,0" />

                                                <Label Grid.Row="2" Grid.Column="0"
                                                       Text="{Binding SafetyFlags, StringFormat='⚠️ Alerte sécurité: {0}'}"
                                                       FontSize="12"
                                                       TextColor="Red" 
                                                       Margin="0,8,0,0" 
                                                       FontAttributes="Bold" />

                                                <Frame Grid.Row="0" Grid.Column="1" Grid.RowSpan="3"
                                                       BackgroundColor="Purple"
                                                       Padding="12,6"
                                                       CornerRadius="15"
                                                       VerticalOptions="Center">
                                                    <Label Text="⚠️ Sécurité"
                                                           TextColor="White"
                                                           FontSize="13"
                                                           FontAttributes="Bold" />
                                                </Frame>
                                            </Grid>

                                            <BoxView BackgroundColor="{StaticResource Gray200}" HeightRequest="1" />
                                            <Grid Padding="15,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>

                                                <Button Grid.Column="0"
                                                        Text="✅ Spot sûr"
                                                        BackgroundColor="Green"
                                                        TextColor="White"
                                                        FontSize="13"
                                                        Padding="8,10"
                                                        CornerRadius="8"
                                                        Margin="0,0,5,0"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SpotValidationViewModel}}, Path=CompleteSafetyReviewCommand}"
                                                        CommandParameter="True" />

                                                <Button Grid.Column="1"
                                                        Text="⚠️ Dangereux"
                                                        BackgroundColor="Red"
                                                        TextColor="White"
                                                        FontSize="13"
                                                        Padding="8,10"
                                                        CornerRadius="8"
                                                        Margin="5,0,0,0"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SpotValidationViewModel}}, Path=CompleteSafetyReviewCommand}"
                                                        CommandParameter="False" />
                                            </Grid>
                                        </StackLayout>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </StackLayout>
                </Frame>
            </StackLayout>

        </StackLayout>
    </ScrollView>

</ContentPage>