<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="SubExplore.Views.Settings.AboutPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SubExplore.ViewModels.Settings"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:controls="clr-namespace:SubExplore.Views.Controls"
             x:DataType="vm:AboutViewModel"
             Title="{Binding Title}"
             BackgroundColor="{AppThemeBinding Light={StaticResource Background}, Dark={StaticResource OffBlack}}"
             Shell.NavBarIsVisible="False"
             Shell.TabBarIsVisible="False">

    <ContentPage.Resources>
        <ResourceDictionary>
            <toolkit:InvertedBoolConverter x:Key="InvertBoolConverter" />
            
            <!-- Section Card Style -->
            <Style x:Key="SectionCardStyle" TargetType="Frame">
                <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource Gray800}}" />
                <Setter Property="BorderColor" Value="{AppThemeBinding Light={StaticResource BorderColor}, Dark={StaticResource Gray700}}" />
                <Setter Property="HasShadow" Value="False" />
                <Setter Property="CornerRadius" Value="12" />
                <Setter Property="Padding" Value="16" />
            </Style>
            
            <!-- Section Header Style -->
            <Style x:Key="SectionHeaderStyle" TargetType="Label">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="TextColor" Value="{StaticResource TextPrimary}" />
                <Setter Property="Margin" Value="0,0,0,12" />
            </Style>
            
            <!-- Info Item Style -->
            <Style x:Key="InfoItemStyle" TargetType="Label">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="TextColor" Value="{StaticResource TextSecondary}" />
                <Setter Property="Margin" Value="0,4,0,4" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <!-- Custom Navigation Bar -->
        <controls:CustomNavigationBar Title="À Propos" 
                                      VerticalOptions="Start"
                                      ZIndex="100" />
        
        <Grid RowDefinitions="Auto,*" Margin="0,56,0,0">

        <!-- Header Section -->
        <Frame Grid.Row="0" 
               Padding="15"
               BackgroundColor="{AppThemeBinding Light={StaticResource Background}, Dark={StaticResource OffBlack}}"
               BorderColor="Transparent">
            <StackLayout Spacing="15">

                <!-- App Header Card -->
                <Frame BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}" 
                       CornerRadius="12" 
                       HasShadow="False" 
                       Padding="16">
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="16">
                        
                        <!-- App Icon -->
                        <Frame Grid.Column="0"
                               BackgroundColor="{AppThemeBinding Light=White, Dark={StaticResource Gray800}}"
                               CornerRadius="30"
                               Padding="8"
                               WidthRequest="60"
                               HeightRequest="60"
                               HasShadow="False">
                            <Image Source="icone_subexplore.svg"
                                   WidthRequest="44"
                                   HeightRequest="44"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center" />
                        </Frame>
                        
                        <!-- App Info -->
                        <StackLayout Grid.Column="1" 
                                   VerticalOptions="Center" 
                                   Spacing="4">
                            <Label Text="{Binding AppName}"
                                   FontSize="20"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=White, Dark={StaticResource PrimaryDarkText}}" />
                            <Label Text="{Binding AppVersion, StringFormat='Version {0}'}"
                                   FontSize="14"
                                   TextColor="{AppThemeBinding Light=White, Dark={StaticResource PrimaryDarkText}}"
                                   Opacity="0.9" />
                            <Label Text="{Binding AppDescription}"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=White, Dark={StaticResource PrimaryDarkText}}"
                                   Opacity="0.8"
                                   LineBreakMode="WordWrap" />
                        </StackLayout>
                    </Grid>
                </Frame>
            </StackLayout>
        </Frame>

        <!-- Content Area -->
        <ScrollView Grid.Row="1">
            <StackLayout Padding="16" Spacing="16">

                <!-- App Information Section -->
                <Frame Style="{StaticResource SectionCardStyle}">
                    <StackLayout Spacing="12">
                        <Label Text="Informations sur l'application" 
                               Style="{StaticResource SectionHeaderStyle}" />

                        <Grid RowDefinitions="Auto,Auto" 
                              ColumnDefinitions="Auto,*" 
                              RowSpacing="8" 
                              ColumnSpacing="12">
                            
                            <Label Grid.Row="0" Grid.Column="0" Text="Version :" FontAttributes="Bold" Style="{StaticResource InfoItemStyle}" />
                            <Label Grid.Row="0" Grid.Column="1" Text="{Binding AppVersion}" Style="{StaticResource InfoItemStyle}" />
                            
                            <Label Grid.Row="1" Grid.Column="0" Text="Mise à jour :" FontAttributes="Bold" Style="{StaticResource InfoItemStyle}" />
                            <Label Grid.Row="1" Grid.Column="1" Text="Décembre 2024" Style="{StaticResource InfoItemStyle}" />
                        </Grid>
                    </StackLayout>
                </Frame>

                <!-- Mission Section -->
                <Frame Style="{StaticResource SectionCardStyle}">
                    <StackLayout Spacing="12">
                        <Label Text="Notre Mission" 
                               Style="{StaticResource SectionHeaderStyle}" />

                        <Label Text="SubExplore a été créé par et pour la communauté des sports sous-marins. Notre objectif est de faciliter la découverte de nouveaux sites de plongée tout en privilégiant la sécurité et le partage d'expériences entre passionnés."
                               Style="{StaticResource InfoItemStyle}"
                               LineBreakMode="WordWrap" />
                               
                        <Label Text="Chaque spot partagé contribue à enrichir notre base de données collective, permettant à tous les plongeurs de bénéficier des connaissances de la communauté."
                               Style="{StaticResource InfoItemStyle}"
                               LineBreakMode="WordWrap" />
                    </StackLayout>
                </Frame>

                <!-- Features Section -->
                <Frame Style="{StaticResource SectionCardStyle}">
                    <StackLayout Spacing="12">
                        <Label Text="Fonctionnalités principales" 
                               Style="{StaticResource SectionHeaderStyle}" />

                        <StackLayout Spacing="10">
                            <Label Text="• Cartographie interactive des sites de plongée"
                                   Style="{StaticResource InfoItemStyle}" />
                            
                            <Label Text="• Ajout et partage de vos spots préférés"
                                   Style="{StaticResource InfoItemStyle}" />
                            
                            <Label Text="• Informations détaillées : profondeur, courant, visibilité"
                                   Style="{StaticResource InfoItemStyle}" />
                            
                            <Label Text="• Système de favoris pour retrouver vos sites"
                                   Style="{StaticResource InfoItemStyle}" />
                            
                            <Label Text="• Validation communautaire pour garantir la qualité"
                                   Style="{StaticResource InfoItemStyle}" />
                            
                            <Label Text="• Statistiques personnelles de vos plongées"
                                   Style="{StaticResource InfoItemStyle}" />
                            
                            <Label Text="• Profils utilisateur et gestion des préférences"
                                   Style="{StaticResource InfoItemStyle}" />
                        </StackLayout>
                    </StackLayout>
                </Frame>

                <!-- Safety Section -->
                <Frame Style="{StaticResource SectionCardStyle}">
                    <StackLayout Spacing="12">
                        <Label Text="Sécurité et Responsabilités" 
                               Style="{StaticResource SectionHeaderStyle}" />

                        <Label Text="SubExplore facilite le partage d'informations entre plongeurs, mais ne remplace jamais :"
                               Style="{StaticResource InfoItemStyle}"
                               LineBreakMode="WordWrap" />

                        <StackLayout Spacing="8" Margin="16,0,0,0">
                            <Label Text="• Une formation de plongée certifiée"
                                   Style="{StaticResource InfoItemStyle}" />
                            
                            <Label Text="• La vérification des conditions locales"
                                   Style="{StaticResource InfoItemStyle}" />
                            
                            <Label Text="• L'évaluation personnelle de vos compétences"
                                   Style="{StaticResource InfoItemStyle}" />
                            
                            <Label Text="• Le respect des réglementations locales"
                                   Style="{StaticResource InfoItemStyle}" />
                        </StackLayout>

                        <Label Text="Plongez toujours accompagné et dans les limites de votre certification."
                               Style="{StaticResource InfoItemStyle}"
                               LineBreakMode="WordWrap"
                               FontAttributes="Italic"
                               TextColor="{StaticResource Warning}" />
                    </StackLayout>
                </Frame>

                <!-- Legal Information Section -->
                <Frame Style="{StaticResource SectionCardStyle}">
                    <StackLayout Spacing="12">
                        <Label Text="Mentions légales" 
                               Style="{StaticResource SectionHeaderStyle}" />

                        <StackLayout Spacing="12">
                            <Label Text="{Binding LegalDisclaimer}" 
                                   Style="{StaticResource InfoItemStyle}"
                                   LineBreakMode="WordWrap" />
                                   
                            <Label Text="{Binding Copyright}" 
                                   Style="{StaticResource InfoItemStyle}"
                                   FontSize="12"
                                   HorizontalOptions="Center" />
                        </StackLayout>
                    </StackLayout>
                </Frame>

                <!-- Action Buttons -->
                <StackLayout Spacing="12" Margin="0,8,0,16">
                    
                    <!-- Contact Button -->
                    <Button Text="Nous contacter"
                            Command="{Binding ContactCommand}"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                            TextColor="{AppThemeBinding Light=White, Dark={StaticResource PrimaryDarkText}}"
                            CornerRadius="8"
                            FontSize="14"
                            FontAttributes="Bold"
                            HeightRequest="44" />
                    
                    <!-- Privacy Policy Button -->
                    <Button Text="Politique de confidentialité"
                            Command="{Binding PrivacyPolicyCommand}"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Secondary}, Dark={StaticResource Secondary}}"
                            TextColor="{AppThemeBinding Light=White, Dark={StaticResource PrimaryDarkText}}"
                            CornerRadius="8"
                            FontSize="14"
                            FontAttributes="Bold"
                            HeightRequest="44" />
                    
                    <!-- Back Button -->
                    <Button Text="← Retour"
                            Command="{Binding GoBackCommand}"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource Gray800}}"
                            TextColor="{StaticResource TextPrimary}"
                            BorderColor="{AppThemeBinding Light={StaticResource BorderColor}, Dark={StaticResource Gray700}}"
                            BorderWidth="1"
                            CornerRadius="8"
                            FontSize="14"
                            FontAttributes="Bold"
                            HeightRequest="44" />
                </StackLayout>
            </StackLayout>
        </ScrollView>
        </Grid>
        
    </Grid>
</ContentPage>