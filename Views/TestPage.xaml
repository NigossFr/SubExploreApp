<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SubExplore.Views.TestPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Page de Test"
    BackgroundColor="White">

    <StackLayout Padding="20" Spacing="20" VerticalOptions="Center">
        
        <Label Text="PAGE DE TEST"
               FontSize="32"
               FontAttributes="Bold"
               TextColor="Blue"
               HorizontalOptions="Center" />
        
        <Label Text="Ceci est une page de test pour vérifier que l'application fonctionne"
               FontSize="16"
               TextColor="Gray"
               HorizontalOptions="Center" />

        <Button Text="BOUTON DE TEST"
                BackgroundColor="Blue"
                TextColor="White"
                FontSize="16"
                CornerRadius="8"
                HeightRequest="50" />
    </StackLayout>
</ContentPage>